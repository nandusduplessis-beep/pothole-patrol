# PotholeInput

One-sentence: The app's signature entry — a recessed irregular asphalt hole that doubles as a tap-target. Tapping triggers the camera; tapping the small "address" affordance opens text entry.

```jsx
<PotholeInput onSnap={openCamera} />
```

There's only ever ONE per screen. It IS the screen.
