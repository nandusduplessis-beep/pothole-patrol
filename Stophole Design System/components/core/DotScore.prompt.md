# DotScore

Row of N filled / unfilled dots in the verdict colour. Quantity *is* the argument.

```jsx
<DotScore score={7} total={10} verdict="green" />
<DotScore score={3} total={10} verdict="red" size={10} />
```
