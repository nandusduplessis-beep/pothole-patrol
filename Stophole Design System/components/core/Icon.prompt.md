# Icon

One-sentence: A 2px stroke Lucide-style icon — `currentColor` so it inherits text color and theme.

```jsx
<Icon name="camera" size={22} />
<Icon name="chevron-right" />
```

Defaults: 18px size, 2px stroke. Use 20 inside buttons, 24 standalone. Never below 16.
